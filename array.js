function panggilArray(){
    var data = [1,2,3,4,5,9];
    var nama = ['abi', 'abi1', 'abi2'];

    console.log(data.length);
    console.log(nama.length);
    console.log();

    for(var i=0; i<nama.length; i++){
        console.log(nama[i]);
    }
}

panggilArray();
